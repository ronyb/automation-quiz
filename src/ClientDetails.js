class ClientDetails {
    
    constructor(fullName, role, company, email) {
      this.fullName = fullName;
      this.role = role;
      this.company = company;
      this.email = email;
    }
}

export default ClientDetails;